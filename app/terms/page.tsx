import { Footer } from '@/components/Footer';
import { TermsPageComponent } from '../../components/(terms)/terms-page';

export default function About() {
    return (
        <div>
            <TermsPageComponent />
            <Footer />
        </div>
    )
}